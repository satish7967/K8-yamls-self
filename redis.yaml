apiVersion: v1
kind: Pod
metadata:
    name: redis-pod
    labels:
        app: redis
        env: prod
        type: backend
spec:
    containers:
        - name: redis-container
          image: redis

